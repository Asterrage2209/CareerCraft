services:
  - type: web
    name: careercraft
    env: node
    buildCommand: |
      cd Frontend && 
      npm install && 
      npm run lint --if-present &&  # Add a lint step to catch these issues
      npm run build && 
      cd ../Backend && 
      npm install
    startCommand: cd Backend && node server.js